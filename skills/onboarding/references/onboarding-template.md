# ONBOARDING.md template

Use this structure when writing `ONBOARDING.md` at the repo root. Replace every
`{{...}}` with real findings. Delete a section only if it genuinely does not
apply, and note why in one line where it would have been.

---

```markdown
# Onboarding: {{project name}}

> Generated by the `onboarding` skill on {{date}} against commit {{short SHA}}.
> Read-only snapshot — verify commands against the current source before relying on them.

## 1. What this project is

{{2–4 sentences: purpose, who it's for, what problem it solves. Pull from README
and package description.}}

**Stack:** {{primary language + framework}} — {{other languages/tooling}}
**Package manager / task runner:** {{e.g. pnpm, Poetry, Makefile}}
**Repo shape:** {{single package | monorepo with N packages | ...}}

## 2. Quickstart

> Commands extracted from {{sources}}. Not executed by the generator.

```bash
# Install
{{install command}}

# Build
{{build command}}

# Run
{{run command}}

# Test
{{test command}}
```

**Environment**
- {{required env var}} — {{what it's for}} (from `.env.example`)
- ...

**External services required locally**
- {{service}} — {{how it's provided, e.g. docker-compose}}

## 3. Architecture

{{Prose overview: the main components and how a request/operation flows through
them.}}

```mermaid
flowchart TD
    {{component diagram}}
```

**Key modules**
| Module / path | Responsibility |
|---|---|
| `{{path}}` | {{what it owns}} |

**Key abstractions**
- `{{type/class/interface}}` (`{{path}}`) — {{role}}

**External integrations**
- {{database / queue / API}} — {{how it's used}}

## 4. Conventions

- **Testing:** {{framework, where tests live, how to run a subset}}
- **Lint / format:** {{tools + config}}
- **CI:** {{what runs on PR, from which config}}
- **Branch / PR workflow:** {{from CONTRIBUTING, if present}}
- **Commit style:** {{conventional commits? etc.}}

## 5. Start here — reading list

1. `{{path}}` — {{why it's a good first read}}
2. ...

## 6. Where the action is

{{From git history: actively changed areas, and anything that looks dormant.}}

## 7. Glossary

- **{{term}}** — {{plain-language definition}}

## ⚠️ Docs that may be stale

{{Mismatches found between docs and code — stale commands, missing env vars,
documented-but-absent scripts. Empty section is a good sign; say "none found".}}
```
